# Dashboard Financeiro IA - Correção Definitiva V2

Correção aplicada:
- A variável correta `ARQ_EXCEL` agora localiza automaticamente o arquivo `.xlsx`.
- O app procura o Excel na mesma pasta do `app.py` e em subpastas.
- O app mostra na lateral o nome do Excel carregado.
- Removida a validação antiga que causava o erro.

Arquivos esperados no GitHub:
- app.py
- requirements.txt
- dashboard_financeiro_avancado (1).xlsx
